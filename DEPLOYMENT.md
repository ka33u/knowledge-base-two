# 生产化部署说明

当前项目既可直接作为浏览器本地优先版使用，也提供 Node.js 25+ 的局域网服务模式。局域网模式使用 SQLite、服务器附件目录、HttpOnly 会话 Cookie 与服务端角色校验，适合受信任内网的小团队。

## 局域网部署建议

- 当前实现：`npm run dev`，Node.js 内置 HTTP + SQLite
- 数据库：SQLite 起步，资料量增长后迁移 PostgreSQL
- 附件：服务器磁盘或 MinIO 对象存储
- 身份：账号密码（scrypt 哈希）+ HttpOnly 会话 Cookie
- 权限：管理员、编辑者、访客；服务端在每个读写接口校验
- 备份：每日数据库备份，附件目录增量备份

## 云端多人协作建议

- 数据库：PostgreSQL；资料、分类、用户、角色、审计日志独立建表
- 附件：S3 兼容对象存储，使用短期签名上传/下载 URL
- 身份：企业 SSO（OIDC/SAML）或账号密码 + 双因素认证
- 权限：基于工作区、资料分类和资料级 ACL 的服务端授权
- 审计：写入、删除、恢复、权限变更均写入不可修改的服务端审计表
- 运维：HTTPS、限流、病毒扫描、附件大小限制、监控告警与异地备份

## 必须迁移到服务端的能力

1. 用户账号、密码、会话与多因素认证。
2. 真实权限校验、分类/资料 ACL 与共享链接控制。
3. PDF/PPTX/图片等二进制附件的持久化存储与下载控制。
4. 多人编辑冲突处理、版本历史、服务端全文索引和审计日志。

在选择部署方式后，可据此将现有界面逐步接入 API，而不改变当前资料阅读与导入体验。
